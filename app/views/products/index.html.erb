<div class="container-fluid">
  <div class="row mb-4">
    <div class="col-md-12">
      <div class="d-flex justify-content-between align-items-center">
        <h1 class="text-center w-100">Products</h1>
        <%= link_to "Add Product", new_product_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-md-2 me-5">
      <div class="card p-4">
        <h5 class="card-title mb-4">Filter By</h5>
        <%= search_form_for @q, url: products_path, method: :get, local: true, class: "filter-form" do |f| %>
          <div class="mb-3">
            <%= f.label :name_cont, "Search by Name", class: "form-label" %>
            <%= f.search_field :name_cont, class: "form-control" %>
          </div>
          <div class="mb-3">
            <%= f.label :price_gteq, "Min Price", class: "form-label" %>
            <%= f.number_field :price_gteq, class: "form-control", step: "0.01" %>
          </div>
          <div class="mb-3">
            <%= f.label :price_lteq, "Max Price", class: "form-label" %>
            <%= f.number_field :price_lteq, class: "form-control", step: "0.01" %>
          </div>
          <div class="mb-3">
            <%= f.label :s, "Sort By", class: "form-label" %>
            <%= f.select :s, [['Price Low to High', 'price asc'], ['Price High to Low', 'price desc'], ['Name A to Z', 'name asc']], {}, class: "form-control" %>
          </div>
          <%= f.submit 'Apply Filters', class: "btn btn-primary w-100" %>
        <% end %>
      </div>
    </div>
    <div class="col-md-9">
      <%= render "card" %>
      <%= will_paginate @products, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>

    </div>
  </div>
</div>
